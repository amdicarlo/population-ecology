bogus.mean <- function(x, cheat) {
  sum.of.x <- sum(x)
  n <- length(x)
  true.mean <- sum.of.x / n
  (1 + cheat) * true.mean
}

real.sales <- c(1:10)
adjust <- 0.1

#x = real.sales
#cheat = adjust

#bogus.mean(real.sales, adjust)
#bogus.mean(x = real.sales, cheat = adjust)
#bogus.mean(x, cheat)

#####################################
